# Backend

Backend for the Time Focus App.

## Run

cd apps/backend
npm install
npm start

## Environment

Create a .env file with:

PORT=5000
MONGO_URI=mongodb://localhost:27017/timefocus
JWT_SECRET=your_jwt_secret_here
