// Hook to fetch and return all schedule blocks
const useSchedule = () => {
  // TODO: implement API call to GET /api/blocks
  return { blocks: [], loading: false, error: null };
};

export default useSchedule;
